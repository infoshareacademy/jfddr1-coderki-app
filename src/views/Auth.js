import React from 'react';

const Auth = () => (
  <div>
    <p>To jest strona logowania</p>
    <button
      onClick={() => {
        console.log('klik');
      }}
    >
      Klik
    </button>
  </div>
);

export default Auth;
